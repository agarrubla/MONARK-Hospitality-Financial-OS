{
  "name": "monark-financial-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MONARK Hospitality Financial OS — backend (Phase 1: schema, integrity triggers, RLS/RBAC, financial integrity test suite)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "db:migrate": "tsx scripts/migrate.ts",
    "db:reset": "tsx scripts/migrate.ts --reset",
    "test": "vitest run",
    "test:t000": "vitest run tests/t000.critical.test.ts",
    "typecheck": "tsc --noEmit",
    "start": "tsx api/src/main.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "@fastify/cors": "^11.3.0",
    "fastify": "^5.2.0",
    "pg": "^8.13.1",
    "tsx": "^4.19.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
